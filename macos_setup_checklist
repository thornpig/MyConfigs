# Chrome
  * install and set as default
  * install extensions: adblocker, the (face) suspender
  * facebook internal: set bunny as serach engine

# iTerm2
  * install
  * download iTerm2 schemes
  * setup iTerm2 preferences
    - hybrid colorscheme
    - Monaco 18 font
    - left option key as esc+
    - allow clipboard access
    - set theme in appearance to Minimal

# install homebrew

# oh-my-zsh 
  * install
  * sync .zshrc

# neovim
  * install
  * sync .vimrc
  * sync .config/nvim/init.vim

# tmux
  * install
  * sync .tmux.conf
  * in iTerm2 set left option key as esc+

# install fzf (including key bindings and fuzzy completion)

# sync .ssh/config

# install eternal terminal

# macos general settings
  * dock install hide/show
    - defaults write com.apple.dock autohide-delay -int 0;defaults write com.apple.dock autohide-time-modifier -float 1;killall Dock
  * keyboard fast repeat and short delay
  * watch unlock

